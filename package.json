{
  "name": "Qr Code Scanner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "build": "npm run sitemap && vite build",
    "preview": "vite preview",
    "secure:main": "rollup -c rollup.config.mjs"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^1.0.0",
    "rollup": "^4.59.0",
    "vite": "^8.0.0"
  }
}
